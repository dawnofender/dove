#ifndef DMETADATA_HPP
#define DMETADATA_HPP

#include <string>

struct Metadata {
    std::string filename;
    // std::vector<int8_t> data;
    // Metadata(std::vector<int8_t> d = {})
    //     : data(d) {
    //
    // }
};


#endif
